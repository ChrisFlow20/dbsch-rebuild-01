package com.wisecoders.dbs.project.governance.store;

import com.wisecoders.dbs.sys.DoNotObfuscate;

@DoNotObfuscate
public enum VxKey {
  iterator, validator, field, filter, parameter, on, name, value, pattern, dbms;
}
